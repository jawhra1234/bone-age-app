services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./bone-age-ui
      dockerfile: Dockerfile.frontend
    ports:
      - "3000:80"
